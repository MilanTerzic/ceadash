# Route economics risk note

The current route-opportunity model combines a market spread with public auction-result data. Public auction totals should not be interpreted as executable trader capacity.

Until capacity semantics are corrected, route opportunities should be treated as indicative spread analytics only. Any displayed potential margin should be expressed per MW without multiplying by public allocated capacity, unless the application has an explicit user-owned capacity position or a verified remaining-capacity measure.
